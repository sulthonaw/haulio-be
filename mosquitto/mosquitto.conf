# Development broker only. The Compose mapping is loopback-only; use mqtts://,
# per-device auth/ACLs and preferably mTLS with a managed or hardened broker in production.
persistence true
persistence_location /mosquitto/data/
listener 1883
allow_anonymous true
